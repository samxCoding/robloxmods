Load import System
requires; RedTiger
    while running;
        load RedTiger
        if RedTiger is loaded?
        create_code; 4195
        wait for code to be accessed
        (
                Lodge Mania Install

            ModLoader_Max requires Intel_CoreI5 CPU

            lock code; time: partial

            partial = when partial is required 
            when user is matian loaded = Input
        )
    